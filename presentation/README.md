# Presentation

Edit `presentation.tex` — search for `[Student 2]` or `[Student 3]` to find your placeholders.

Fill in: task, dataset, results table, key findings, conclusion.

To compile:
```bash
bash render.sh
```
