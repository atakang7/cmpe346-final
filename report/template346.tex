\documentclass[a4paper,12pt]{report}

\usepackage{styles/ibu-thesis}
\usepackage{subcaption}
\usepackage{booktabs}

\thesistitle{Text Classification in NLP: Media Bias Detection, [Student 2 Topic], and [Student 3 Topic]}

\authors[1]{Atakan G\"{u}l}
\authorsid[1]{121200152}
\authors[2]{[Student 2 Name]}
\authorsid[2]{[Student 2 ID]}
\authors[3]{[Student 3 Name]}
\authorsid[3]{[Student 3 ID]}

\assignment{FINAL PROJECT}
\course{CMPE346: NATURAL LANGUAGE PROCESSING}
\submissiondate{June 2026}

\abstract{
In this paper, we performed text classification on three different NLP tasks using an identical deep learning model. Each task is an application of classification in the NLP field: Media bias detection of news articles, [Student 2 task], and [Student 3 task]. Three models are applied to each task, including one traditional deep learning model Bidirectional LSTM, and two transformer-based pre-trained models BERT and RoBERTa. The three models are then fine-tuned on the dataset for each specific task, and accuracy and weighted F1-score are used as evaluation metrics. For all three tasks, we confirmed that transformer-based models outperform Bidirectional LSTM, and RoBERTa attains the best results.
}

\begin{document}

\maketitle

\addcontentsline{toc}{section}{\abstractname}
\makeabstract

\addcontentsline{toc}{section}{\contentsname}
\maketableofcontents

\pagenumbering{arabic}
\setcounter{page}{1}

%% ─────────────────────────────────────────────
\section*{Introduction}
\addcontentsline{toc}{section}{\textsc{Introduction}}
Text classification is one of the foundational tasks in NLP and underpins many practical applications we use on a daily basis. At its most basic, text classification assigns a label from a predetermined set to an instance of text based on its contents, providing a pathway to automating understanding over large scales. These applications include, but are not limited to: finding misinformation and media bias in news; classifying the argumentative intent of scientific documents; and classifying harmful content in social media.

While the formulation of text classification may be seemingly simple (the assignment of a discrete label to an item of text), the complexities of human language provide a variety of challenges to be addressed in text classification such as learning the semantics and contextuality of words. Early techniques relied on manually engineered features, shallow machine learning models such as Naive Bayes and Support Vector Machines \cite{joachims1998}, and more recently recurrent neural networks, particularly Long Short-Term Memory networks \cite{hochreiter1997}, began learning representations directly from the text. Most impactfully however, was the introduction of the Transformer \cite{vaswani2017} and subsequently, the pre-training and subsequent release of very large scale language models such as BERT \cite{devlin2019} and RoBERTa \cite{liu2019}.

This project looks at text classification in three different NLP tasks each corresponding to a different area of application. The group then proceeds to individually train three different models (BiLSTM, BERT, RoBERTa) on three individually chosen datasets and reports their performance in terms of accuracy and weighted F1-score. The intention of this is to compare how the three models behave under different circumstances.
\newpage

%% ─────────────────────────────────────────────
\chapter{Atakan G\"{u}l --- Media Bias Detection in News}

\section{Methodology}

This work targets the task of media bias detection from news articles. It is a classification task where the model has to decide whether a news sentence has biased framing or no bias. Media bias detection is a non-trivial NLP problem because bias is manifested by subtler things like phrasing, framing and tone, and not by stating false facts \cite{hamborg2019}.

In this work, 3 models are tested:

\textbf{Bidirectional LSTM.} A Long Short-Term Memory network is a type of recurrent neural network designed to remember information for long periods of time \cite{hochreiter1997}. Bidirectional LSTMs are trained in both directions in order to make better use of the context. In our work the model takes an embedding layer, two layers of 256-hidden-dimension Bidirectional LSTMs and a linear classification layer on top of the concatenation of the last forward and backward hidden states.

\textbf{BERT.} BERT is a pre-trained language model based on the Transformer encoder architecture. It is trained on a large corpus using a masked language modeling objective and a next sentence prediction objective and therefore is able to produce deep bidirectional context representations \cite{devlin2019}. We feed the \texttt{[CLS]} token output through a linear classifier. For the implementation of BERT we use the \texttt{bert-base-uncased} variant.

\textbf{RoBERTa.} The Robustly Optimized BERT Pretraining Approach (RoBERTa) is a variant of BERT in which the next sentence prediction task was removed, trained on a larger corpus using larger batch size and uses dynamic masking \cite{liu2019}. This architecture has been shown to perform consistently better on downstream tasks. For RoBERTa we use the \texttt{roberta-base} variant.

The transformer models (BERT and RoBERTa) are fine-tuned from the beginning with an AdamW optimizer and a linear learning rate scheduler with warmup. The LSTM model is trained from scratch using the Adam optimizer.

\newpage

\section{Experiments \& Findings}
\textbf{Dataset.} The BABE dataset (Bias Annotations By Experts) \cite{spinde2021} is employed for this task. It is composed of 4,121 news sentences gathered from a diverse collection of sources, each rated as either biased (1) or not biased (0) by trained annotators. It is divided into 3,121 training and 1,000 test samples. Since the labeling is conducted with the help of expert annotators and has been designed carefully, it serves as a reliable reference for media bias detection.

\textbf{Training Configuration.} For all models, 3 epochs of training are performed with early stopping on F1 score on the validation set, a stopping threshold of 0.5\% difference between consecutive performances and a patience of 2 epochs. A batch size of 16 and a maximum sequence length of 128 are set. The learning rate for transformers is $2 \times 10^{-5}$ and for the LSTM is $1 \times 10^{-3}$. All models are trained on an NVIDIA GeForce RTX 3060 Laptop GPU.

\textbf{Results.} Table \ref{tab:atakan_results} presents the performance of each model on the BABE test set.
\begin{table}[h]
    \centering
    \begin{tabular}{lcc}
        \toprule
        \textbf{Model} & \textbf{Accuracy} & \textbf{F1} \\
        \midrule
        LSTM      & 0.6840 & 0.6772 \\
        BERT      & 0.8120 & 0.8122 \\
        RoBERTa   & 0.8580 & 0.8585 \\
        \bottomrule
    \end{tabular}
    \caption{Model performance on the BABE dataset for media bias detection.}
    \label{tab:atakan_results}
\end{table}

\newpage

\section{Discussion}

From these figures, it is clear that there is a performance hierarchy across the three architectures: The Bidirectional LSTM gets 68.4% accuracy and an F1 score of 0.677 which although greater than random are nowhere near the performances of the transformer architectures. This difference shows the difficulty for a recurrent architecture, pre-trained from scratch, to learn both linguistic features and the relevant task-specific patterns from an relatively small amount of data.

BERT scores well, gaining 81.2% accuracy and an F1 score of 0.812 showing the success of transfer learning, where the pre-trained models contain deep representations of language learned on a huge corpus of text that have only to be refined and adapted on the smaller, labeled data set.

RoBERTa scores an excellent 85.8% accuracy and 0.858 F1 score. This result is comparable with those shown in the RoBERTa paper \cite{liu2019} and could be seen as proof that the improvements to the pre-training procedure: the removal of the next sentence prediction, the addition of dynamic masking and generally greater attention paid to the masking scheme, results in better-tuned and consequently better-performing representations on downstream, fine-tuning tasks.

\newpage

\section{Conclusion}

This paper applied three deep learning architectures to detect media bias using the BABE dataset. Experiments indicated that the pretrained transformers substantially outperformed the LSTM baseline, and RoBERTa produced the best results with accuracy 85.8\% and weighted F1 0.858. The experimental results also indicate the crucial role of pretraining for low-resource classification problems since it is not possible to learn the nuances of language for news framing and bias without pretraining. Further investigations could use larger transformer models or domain adaptation pretraining on news corpora.

%% ─────────────────────────────────────────────
\chapter{[Student 2 Name] --- [Student 2 Topic]}

\section{Methodology}
\textit{[Student 2 fills this in]}
\newpage

\section{Experiments \& Findings}
\textit{[Student 2 fills this in]}
\newpage

\section{Discussion}
\textit{[Student 2 fills this in]}
\newpage

\section{Conclusion}
\textit{[Student 2 fills this in]}

%% ─────────────────────────────────────────────
\chapter{[Student 3 Name] --- [Student 3 Topic]}

\section{Methodology}
\textit{[Student 3 fills this in]}
\newpage

\section{Experiments \& Findings}
\textit{[Student 3 fills this in]}
\newpage

\section{Discussion}
\textit{[Student 3 fills this in]}
\newpage

\section{Conclusion}
\textit{[Student 3 fills this in]}

%% ─────────────────────────────────────────────
\clearpage
\bibliographystyle{ieeetr}
\addcontentsline{toc}{section}{References}

\begin{thebibliography}{100}

\bibitem{vaswani2017} A. Vaswani et al., ``Attention is all you need,'' \textit{Advances in Neural Information Processing Systems}, vol. 30, 2017.

\bibitem{devlin2019} J. Devlin, M. Chang, K. Lee, and K. Toutanova, ``BERT: Pre-training of deep bidirectional transformers for language understanding,'' in \textit{Proc. NAACL}, 2019, pp. 4171--4186.

\bibitem{liu2019} Y. Liu et al., ``RoBERTa: A robustly optimized BERT pretraining approach,'' \textit{arXiv preprint arXiv:1907.11692}, 2019.

\bibitem{hochreiter1997} S. Hochreiter and J. Schmidhuber, ``Long short-term memory,'' \textit{Neural Computation}, vol. 9, no. 8, pp. 1735--1780, 1997.

\bibitem{joachims1998} T. Joachims, ``Text categorization with support vector machines: Learning with many relevant features,'' in \textit{Proc. ECML}, 1998, pp. 137--142.

\bibitem{hamborg2019} F. Hamborg, K. Donnay, and B. Gipp, ``Automated identification of media bias in news articles: An interdisciplinary literature review,'' \textit{International Journal on Digital Libraries}, vol. 20, pp. 391--415, 2019.

\bibitem{spinde2021} T. Spinde et al., ``Neural media bias detection using distant supervision with BABE,'' in \textit{Findings of EMNLP}, 2021.

\end{thebibliography}

\end{document}
